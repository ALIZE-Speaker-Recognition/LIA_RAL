# Please fill the correct path (ABSOLUTE) for ALIZE, BIN and LIB dirs
ALIZE_DIR=/home/jfb-local/FormationMistral/src_dev/ALIZE

USER_BIN_DIR=/home/jfb-local/FormationMistral/bin/
USER_LIB_DIR=/home/jfb-local/FormationMistral/lib/

# Package Name
PACK=LIA_RAL_v2

# Executable file extension
EXE_EXT=.exe

# Utilities
CPP=/usr/bin/g++
RM=/bin/rm
AR=/usr/bin/ar
RANLIB=/usr/bin/ranlib

# Put ctags for vi and etags for emacs
TAGS=/usr/bin/ctags
TAGS_DIR=./

# Compiler options
CXXFLAGS_DEBUG=-Wall -pg -ggdb -ansi
LDFLAGS_DEBUG=-Wall -pg -ggdb
CXXFLAGS=-Wall -O3 -ffast-math 
LDFLAGS=-Wall -O3 -ffast-math -static 

# If you want threaded versions of accumulators
THREAD=false

# This is generated by install script
LIA_RAL_TOPDIR=/home/jfb-local/FormationMistral/src_dev/LIA_RAL

LIA_SpkTools_DIR=$(LIA_RAL_TOPDIR)/LIA_SpkTools
LIA_SpkDet_DIR =$(LIA_RAL_TOPDIR)/LIA_SpkDet
LIA_Utils_DIR=$(LIA_RAL_TOPDIR)/LIA_Utils

MAKEFLAGS=-rks
ALIZE_LIB=$(ALIZE_DIR)/lib/
ALIZE_SRC=$(ALIZE_DIR)/src/

# Debug #
ifeq ($(DEBUG),yes)
	CXXFLAGS=$(CXXFLAGS_DEBUG)
	LDFLAGS=$(LDFLAGS_DEBUG)
	ALIZE_LIB=$(ALIZE_DIR)/libdebug/
endif
