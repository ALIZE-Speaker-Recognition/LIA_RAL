#if !defined(ALIZE_FusionScore_h)
#define ALIZE_FusionScore_h

#include "alize.h"
extern bool debug;
extern bool verbose;

        int FusionScore(alize::Config&);
        int FusionClassScore(alize::Config&);

#endif // !defined(FusionScore)
