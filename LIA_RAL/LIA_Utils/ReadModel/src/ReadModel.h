#if !defined(ALIZE_ReadModel_h)
#define ALIZE_ReadModel_h

#include "alize.h"

        int readModel(alize:: Config &);
        int outputWeightVector(alize:: Config &);

#endif // !defined(ReadModel)
