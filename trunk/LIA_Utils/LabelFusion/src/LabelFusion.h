#if !defined(ALIZE_LabelFusion_h)
#define ALIZE_LabelFusion_h

#include "alize.h"
extern bool debug;
extern bool verbose;

        int labelFusion(alize::Config&);
        int labelMorphing(alize::Config&);
#endif // !defined(Scoring)
